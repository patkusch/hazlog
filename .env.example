# Optional. Only the narrow lookup path (make lookup TERM=...) uses it.
# The pipeline and the demo run without it. The corpus is never sent here.
GEMINI_API_KEY=
# Optional overrides for the local path.
# OLLAMA_HOST=http://127.0.0.1:11434
# HAZLOG_MODEL=gemma3
